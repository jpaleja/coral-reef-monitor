README file
Follow the instructions below to load the Coral Reef Monitor web pages

- Once OSgeolive has loaded open the command
- Type ‘sudo service apache2 start’ to start the apache server
- Type ‘cd /var/www/html’ to change the directory to html
- Type ‘sudo mkdir user’ to create a directory called user where you can save the application files
- Type ‘sudo chown user: user’ to change ownership of the new folder so you have the permissions to edit the folder
- Extract all of the files in the ‘Coral Reef Monitor’ application folder to the new user directory